description "prometheus-pdns-rec-exporter"
respawn
respawn limit 10 30

start on (local-filesystem and net-device-up IFACE!=lo)

setuid prometheus

script
    exec /usr/bin/prometheus-pdns-rec-exporter
end script
